name: example-orders-sync

source:
  type: csv
  path: ./orders.csv
  options:
    hasHeader: true

transforms:
  - where: "Amount > 0"
  - select:
      OrderId: OrderId
      Amount: Amount

sink:
  type: json
  path: ./orders-clean.json
  options:
    indented: true
