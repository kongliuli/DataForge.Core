<Page x:Class="DataMigration.Wpf.PluginManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:DataMigration.Wpf"
      xmlns:vm="clr-namespace:DataMigration.Wpf.ViewModel"
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=vm:PluginManagerViewModel, IsDesignTimeCreatable=True}"
      Title="插件管理">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="插件管理" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />
        
        <Button Grid.Row="1" Content="刷新插件" Command="{Binding RefreshCommand}" Margin="0,0,0,10" Width="120" Height="30" />
        
        <DataGrid Grid.Row="2" ItemsSource="{Binding Plugins}" AutoGenerateColumns="False" Margin="0,10,0,0"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100" />
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="200" />
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="200" />
                <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>